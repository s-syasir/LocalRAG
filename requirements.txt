# Install PyTorch for ROCm FIRST (AMD GPU), then install the rest:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/rocm6.2
#   pip install -r requirements.txt

sentence-transformers>=2.7.0
chromadb>=0.5.0
ollama>=0.3.0
gradio>=4.40.0
python-dotenv>=1.0.0

# openai_api.py only
fastapi>=0.110.0
uvicorn>=0.29.0
